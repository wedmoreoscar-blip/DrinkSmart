project_id = "vcbsvuaflefmnhkapoeq"

# All functions use the @supabase/server withSupabase wrapper with
# auth: 'user' mode, which requires a platform-validated JWT (anonymous OK).
# verify_jwt = true is the default; left explicit for visibility.

[functions.parse-menu]
verify_jwt = true

[functions.submit-feedback]
verify_jwt = true

[functions.generate-plan]
verify_jwt = true