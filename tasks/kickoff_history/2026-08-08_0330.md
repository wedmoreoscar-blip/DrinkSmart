# Session Kickoff — frontend redesign, step 2 of 8

Written 2026-08-08 03:30 BST. The task is unchanged from the 2026-08-07 23:20 kickoff — the
redesign was not touched on 2026-08-08. That session built, tested, and hardened the delegation
path the task below tells you to use, including a live parallel run of two implementers.

## Context

The Claude Design handoff bundle is unpacked and committed at `design_handoffs/design_handoff_drinksmart/`.
**Step 1 (token layer) is done and merged.** Steps 2–8 are planned in `tasks/todo.md`, not started.
**Nothing has been rendered in a browser.**

The 2026-08-08 session did no redesign work. It made delegation real:

- `611e546` — `tools/writespec-guard`, a `PreToolUse` hook that **denies** any `traycer agent send`
  whose spec lacks the verbatim `writespec` `scope.md` + `closing.md` blocks. Commissioning is now
  machine-enforced, not advisory.
- `01f3f9f` — merged `main`'s opencode integration (`063ba08`).
- `eabfcdb` — `git push` moved to `permissions.deny`. Pushing is the user's alone.
- `780d404`, `60e44af`, `42f3a51` — `docs/workflows/agent_selection.md` created, versioned, and
  mirrored to `~/.traycer/agent-selection-guide.md` with drift enforced by `tools/check-agent-setup`.

Delegation was exercised end to end three times (haiku, GPT-5.6 Luna, DeepSeek V4 Flash). All three
passed `speccheck`; all three correctly reported blocked verification rather than working around it.

## READ FIRST

- `AGENTS.md`, then `CLAUDE.md`
- `docs/decisions.md` — two new LOCKED entries (agent roles; multi-harness delegation)
- `docs/workflows/agent_selection.md` — which agent runs which role
- `tasks/todo.md` — the step 2–8 plan with per-step acceptance criteria
- `design_handoffs/design_handoff_drinksmart/README.md`, plus `screens/1k-primitives.png` and `.html` for step 2

## Locked constraints that bite immediately

- Reach for the token, not the hex. 19px legibility floor, 56px touch floor, 64px one primary action.
- One accent, no palette. No red, no green. Primary button **outlined, never filled**.
- Restyle the existing `src/components/ui/*` shadcn primitives — do not fork, no new production deps.
- Light mode stays unreachable: `forcedTheme="dark"` in `src/main.tsx` and `LIGHT_THEME_AVAILABLE`
  in `src/pages/Profile.tsx` are both load-bearing.
- Do not read `design_handoffs/design_handoff_drinksmart/DrinkSmart-design-reference.html` (1.3 MB, for a human).
- **Every delegation must be commissioned by a `writespec` spec with the fixed blocks appended
  (`cat`, not retyped) — the hook will reject it otherwise — and accepted only via `speccheck`.**

## Verification baseline — CHANGED, read carefully

**There is no `node_modules` in any checkout.** Confirmed 2026-08-08 in both the main checkout and
fresh worktrees. The previous kickoff's "typecheck PASS" is therefore **not currently reproducible**:

- `npx tsc --noEmit` — **BLOCKED** until `npm install` runs. Was PASS on 2026-08-07.
- `npm run lint` — known FAIL (9 errors, 12 warnings). Must not get worse. Currently BLOCKED.
- `npm run build` — was PASS. Currently BLOCKED.
- Browser, Supabase, and native verification remain BLOCKED regardless.

Run `npm install` first, or state these as BLOCKED in any spec you write. Two implementers hit this
and correctly refused to `npm install` because the spec forbade it — write the baseline accordingly.

## Delegation setup (ready, proven end to end)

- Orchestrator: Claude Code, Opus 5 / Fable 5, `tui`.
- Default implementer: `--harness opencode --model deepseek:deepseek-v4-flash` (colon, not slash).
- Escalation only for visual input or spatial reasoning: `--harness codex --model gpt-5.6-luna`.
  **Step 2 is visual work against reference PNGs — this is a genuine Luna case.**
- Always `--surface gui` (tui a2a is Claude-only) and `--permission-mode auto_accept_edits`.
- **`--reasoning-effort max` is required, not optional.** A/B-tested: without it the agent runs at
  low effort even though `opencode.json` sets `agent.build.variant: "max"`. Config alone does not
  reach a Traycer-launched agent.
- **Confirm `create` returned a clean agent id before dispatching.** A timed-out create
  (`WebSocket frame timed out after 15000ms`) still produces an agent, but with *none* of the flags
  applied — it silently ran `opencode/big-pickle` at default effort once. It fails open.
- Verify what actually ran: `opencode session list` → `opencode export <id>`, check `providerID`
  and `modelID`. Note `variant` in the export is **not** a reliable effort signal — it reads `max`
  either way. Only the GUI shows true effort.
- **Parallel delegation works.** DeepSeek and Luna ran simultaneously in separate worktrees with no
  cross-contamination; both passed. Spawn several DeepSeek implementers when specs are independent.
- One `traycer worktree create` per implementer; delete after `speccheck` passes.
- `tui` agents pin their worktree — kill the process first. A `gui` agent blocked on an approval
  prompt also pins it, with no local process to kill. No CLI agent-delete exists; use the sidebar.
  `traycer agent list` can keep showing agents after the sidebar is cleared — stale, harmless.
- **Invoke the `speccheck` skill properly on real work**: enumerate clauses *before* reading the
  diff, and derive tests from the clause list without looking at the implementation. Informal
  checking was used on tonight's throwaway tests and the ordering drifted.

## Explicit exclusions

- **Do not push.** Enforced by `permissions.deny`. This branch is 7 commits ahead; `main` is ahead
  of `origin/main` deliberately.
- Do not start step 7 (engine work) without raising the test-runner question — it needs
  deterministic tests, no runner exists, and vitest requires explicit approval.
- Not in scope: Profile/onboarding, drink picker, menu scanner, establishment browsing, auth, a
  `DrinksTab.tsx` refactor beyond its meter, re-enabling a light theme.
- `~/.traycer/agent-selection-guide.md` is machine-local and unversioned — edit the repo copy and
  sync, or `check-agent-setup` fails on drift.

## PROMPT

```text
Continue the DrinkSmart frontend redesign at step 2 of tasks/todo.md: restyle the shadcn primitives
in src/components/ui/* to design 1k, and replace the battery meter at DrinksTab.tsx:878-990 with the
1h continuous vessel meter.

Read design_handoffs/design_handoff_drinksmart/README.md (sections "Design tokens" and "1k — Primitives"), look at
screens/1k-primitives.png, and read screens/1k-primitives.html for the exact markup. Port it into the
existing primitives; do not fork them and do not paste HTML.

Before anything else, run npm install — there is no node_modules in this checkout, so typecheck,
lint, and build are all currently BLOCKED and the verification baseline cannot be met without it.

This spans several files, so delegate: write the spec with the writespec skill (appending the fixed
scope/closing blocks with cat — the PreToolUse hook rejects specs without them), dispatch through
Traycer, then verify the returned diff with speccheck. Per docs/workflows/agent_selection.md this is
visual work against reference PNGs, so use GPT-5.6 Luna via codex rather than the DeepSeek default.

State the verification baseline in the spec explicitly: after npm install, typecheck must be PASS,
lint is a known FAIL at 9 errors and 12 warnings and must not get worse, build must PASS, and
browser verification is BLOCKED.

Confirm the plan with the user before dispatching.
```
