# Session Kickoff — frontend redesign, step 2 of 8

Written 2026-08-07 23:20 BST.

## Context

The Claude Design handoff bundle for the DrinkSmart redesign is unpacked and committed at
`design_handoff_drinksmart/`. **Step 1 of its suggested order — the token layer — is done and merged
into `main`.** Steps 2 through 8 are planned in `tasks/todo.md` and not started.

What landed this session:

- `9a48a02` — the handoff bundle, committed as delivered.
- `1e4efb8`, `ed7ecc0` — two in-repo amendments to the bundle's README: the buzz ceiling is level 7,
  not 6, and level 7 gets a fourth band of its own ("Heavy"). Also stopped tracking `.env`.
- `b46de81` — `tokens/index.css` and `tokens/tailwind.config.ts` replace `src/index.css` and
  `tailwind.config.ts`.
- `d1d2be8` — dark forced, Inter self-hosted, light theme wired but unreachable.

**Nothing has been rendered.** `npm run dev` has not been run since the token layer landed. The
redesign is verified statically only.

## READ FIRST

- `AGENTS.md`, then `CLAUDE.md` when using Claude Code
- `docs/decisions.md` — three redesign entries were added this session and constrain every step
- `tasks/todo.md` — the full step 2–8 plan with per-step acceptance criteria
- `design_handoff_drinksmart/README.md` — the spec, self-sufficient
- `design_handoff_drinksmart/screens/1k-primitives.png` and `1k-primitives.html` for step 2

## Locked constraints that bite immediately

- Reach for the token, not the hex. Step 1 put every value in `src/index.css` / `tailwind.config.ts`.
- 19px is the legibility floor; 56px the touch floor; 64px the one primary action per screen.
- One accent, no palette. No red, no green. The primary button is **outlined, never filled**.
- Restyle the existing `src/components/ui/*` shadcn primitives — do not fork them, do not add a
  production dependency without approval.
- Light mode stays unreachable. `forcedTheme="dark"` in `src/main.tsx` and `LIGHT_THEME_AVAILABLE`
  in `src/pages/Profile.tsx` are both load-bearing.
- `design_handoff_drinksmart/DrinkSmart-design-reference.html` is 1.3 MB of compiled output for a
  human. Do not read or parse it.

## Verification baseline

- `npm run typecheck` — PASS as of 2026-08-07. Must stay PASS.
- `npm run lint` — known FAIL: 9 errors, 12 warnings. Must not get worse.
- `npm run build` — PASS at last run. `tools/test-project full` covers both.
- Browser, Supabase, and native device verification are `BLOCKED`. Typecheck and build are not
  functional verification of a visual change.

## Explicit exclusions

- Do not push. `main` is 2 commits ahead of `origin/main` deliberately.
- Do not start step 7 (engine work) without raising the test-runner question first — it needs
  deterministic tests, no runner exists, and adding vitest requires explicit approval.
- Not in scope: Profile/onboarding, drink picker, menu scanner, establishment browsing, auth,
  a `DrinksTab.tsx` refactor beyond its meter, re-enabling a light theme.
- The merged `redesign/token-layer` branch is stale and can be deleted whenever the user wants.

## PROMPT

```text
Continue the DrinkSmart frontend redesign at step 2 of tasks/todo.md: restyle the shadcn primitives
in src/components/ui/* to design 1k, and replace the battery meter at DrinksTab.tsx:878-990 with the
1h continuous vessel meter.

Read design_handoff_drinksmart/README.md (sections "Design tokens" and "1k — Primitives"), look at
screens/1k-primitives.png, and read screens/1k-primitives.html for the exact markup. Port it into the
existing primitives; do not fork them and do not paste HTML.

This spans several files, so per AGENTS.md write the spec with the writespec skill and delegate
through Traycer rather than editing inline, then verify the returned diff with speccheck. State the
verification baseline in the spec: typecheck must stay PASS, lint is a known FAIL at 9 errors and 12
warnings and must not get worse, build must stay PASS, and browser verification is BLOCKED.

Confirm the plan with the user before dispatching.
```
