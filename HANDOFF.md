# Session Handoff — portable integration baseline

The repository contains no active continuation task at this baseline. The canonical continuation is
`tasks/next_session_kickoff.md`. Future normal handoffs should replace this file and that kickoff with
the same current session bundle; archive-only handoffs must leave both untouched.
