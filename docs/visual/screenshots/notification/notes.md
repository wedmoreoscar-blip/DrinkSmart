- `wave3-drinksmart_luna_0-20260811T204019Z-suspect.png` — 2026-08-11 20:40Z — Web toast fallback at 402×874 rendered two 362×64px dark cards with 14px radius and 60px action rows. Definite finding: both `Had it` and `+15 min` action labels computed `rgb(0, 0, 0)` with a black divider instead of accent/foreground roles, making them unreadable on the dark card. Native lock-screen rendering is BLOCKED in browser-only verification.
- `wave3-drinksmart_luna_0-20260811T211015Z-ok.png` — 2026-08-11 21:10Z — Re-shot web toast fallback at 402×874 after the action-role fix. Two cards remain 362×64px with 14px radius; action rows remain 60px/19px. `Had it` computes `rgb(181,171,252)` (#b5abfc), `+15 min` computes `rgb(233,233,237)` (#e9e9ed), and the divider computes `rgb(56,58,70)` (#383a46); document/body widths remain 402px. PASS for the browser-rendered state; native lock-screen rendering remains BLOCKED.
- `wave3-codex-20260811T214226Z-web-fallback-suspect.png` — 2026-08-11 21:42Z — Orchestrator comparison confirmed the corrected action roles and two full-size 362×64px cards after restoring unrelated Sonner defaults, but both action labels wrapped in columns squeezed to 59–73px. FINDING; repaired inline with equal hook-scoped, non-wrapping action columns. Native lock-screen rendering remains BLOCKED.
- `wave3-codex-20260811T215208Z-web-fallback-ok.png` — 2026-08-11 21:52Z — Orchestrator web-fallback milestone with the Sonner stack expanded for inspection. Both actions use equal 72×60px hook-scoped columns and stay on one line; `Had it` is #b5abfc, `+15 min` is #e9e9ed, and the divider is #383a46. The 362px card keeps its 64px minimum and grows to 78.09px when the dynamic description needs a second line instead of clipping copy. PASS for browser fallback; native lock-screen appearance and device action delivery remain BLOCKED on physical hardware.
